name: Run Cirrus Tasks

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ master ]

jobs:
  cirrus:
    runs-on: alpine/local-latest
    steps:
    - uses: actions/checkout@v2
    - uses: cirruslabs/cirrus-action@v2
